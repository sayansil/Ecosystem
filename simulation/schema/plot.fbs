namespace Visualisation;

table Plot {
    key:string (key, required);
    title:string;
    xlabel:string;
    ylabel:string;
    x:[float] (required);
    y:[float] (required);
}


table PlotGroup {
    title:string (key, required);
    plots:[Plot];
}

root_type PlotGroup;
