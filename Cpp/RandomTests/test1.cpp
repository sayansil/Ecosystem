#include <helper.hpp>
#include <fmt/core.h>
#include <vector>

int main()
{
    //std::vector<uint8_t> res = helper::create_bytevector("10010000");
    //for(const auto& i : res)
    //    fmt::print("{}\n", (int)i);

    //std::vector<uint8_t> arr = {144, 0};
    //uint32_t val = helper::get_value_from_bytearray(arr.data(), arr.size(), 2, 3);
    //fmt::print("{}\n", val);
}
